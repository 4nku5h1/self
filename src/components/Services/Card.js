export default function ServiceCard({ title, description }) {

    return (
        <div className='page details'>
            <h2>{title}</h2>
            <p>{description}</p>
        </div>

    )
}